Put song lyrics here as plain text files.

Filename convention:
- 1.txt
- 8.txt

The backend exposes lyric availability in song payloads via `has_lyrics` and full text through `GET /songs/{id}/lyrics`.
